services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: taskpilot
      POSTGRES_USER: taskpilot
      POSTGRES_PASSWORD: taskpilot
    ports:
      - "5432:5432"
    volumes:
      - taskpilot_pg:/var/lib/postgresql/data

volumes:
  taskpilot_pg:

